# Python
__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/

# Tooling
.pytest_cache/
.ruff_cache/
.DS_Store

# Training artefacts — never commit weights or fitted profiles
checkpoints/
runs/
wandb/
*.safetensors
*.bin

# Secrets
.env
.env.*
!.env.example

# Generated data — rebuild with `make data` / `make eval-set`, never commit.
# The manifest records exactly what went in, so the corpus is reproducible
# without carrying hundreds of MB in git.
data/mixture/
data/eval/
